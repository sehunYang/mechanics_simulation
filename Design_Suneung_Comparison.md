# 수능 물리 문항 작도 규격 대조 및 적용

2022~2026학년도 수능 **물리학Ⅰ·Ⅱ** 문항지(한국교육과정평가원 공개 기출) 5개년의
역학 문항 그림을 조사해 작도 관례를 정리하고, 시뮬레이터 요소 디자인을 그 규격으로 바꿨다.

> 참고 자료는 관례 파악에만 사용했고, 원본 그림을 복제하거나 저장소·배포본에 포함하지 않았다.
> 저장소에 들어간 것은 전부 이 규격에 따라 새로 작도한 것이다.
> (작도 관례 자체는 저작권 보호 대상이 아니다 — 보호되는 것은 개별 그림의 표현이다.)

## 1. 조사 대상

| 연도 | 과목 | 확인한 대표 역학 문항 |
|---|---|---|
| 2026 | 물리학Ⅰ | 7번(빗면+벽+충돌), 9번(도르래+천장), 10번(수평면 위 물체열) |
| 2025 | 물리학Ⅰ | 18번(도르래 2개+실+빗면), 20번(용수철+마찰 구간) |
| 2024 | 물리학Ⅰ | 3번(적층), 그 외 수평면 문항 |
| 2023 | 물리학Ⅰ·Ⅱ | 빗면·실·도르래 문항 |
| 2022 | 물리학Ⅰ | 20번(벽+용수철+마찰 구간+빗면+치수선) |

5개년 내내 관례가 **거의 완전히 일관**됐다.

## 2. 대조표

| 요소 | 수능 규격 | 변경 전 | 변경 후 |
|---|---|---|---|
| 전체 톤 | 흰 바탕, 검정 선 + 회색 채움만 | 다크 배경 + 요소별 컬러 | **흰 바탕 완전 흑백** |
| 바닥/지형 | 가는 검정 실선 한 줄 | 회색 선 + 45° 빗금 | **실선 한 줄** |
| 실체면(벽·천장) | 표면에서 멀어지며 흐려지는 회색 띠 | 45° 빗금 | **회색 그라데이션 띠** |
| 마찰 구간 | 표면에 덧댄 진한 회색 띠 + `마찰 구간` 글자 | 빨간 45° 빗금 | **진한 회색 띠 + `마찰`** |
| 물체(사각) | 가는 테두리 + 연회색 채움, 라벨 세리프 | 회색 테두리, 모노스페이스 | **규격 일치** |
| 물체(원) | 가는 테두리 + 연회색 채움 | 동일 계열 | **규격 일치** |
| 용수철 | 나선 코일 (고리가 겹침) | 지그재그 | **나선 코일** |
| 도르래 | 동심원 3겹 + 축핀 + 2줄 요크 브래킷 | 원 2개 + 점 | **규격 일치**, 브래킷은 고정점 방향 |
| 실 | 가는 검정 실선 한 줄 | 주황 선 | **검정 실선** |
| 힘 화살표 | 가는 선 + 속 찬 삼각 화살촉 | 주황 화살표 | **규격 일치** |
| 힘 구간 | (수능 자기장 영역 표기 계열) 연회색 + 점선 테두리 | 반투명 파랑 | **연회색 + 점선** |
| 서체 | 수식 세리프 이탤릭 / 한글 명조 | 모노스페이스 | **규격 일치** |

## 3. 적용하지 않은 관례와 이유

| 관례 | 미적용 사유 |
|---|---|
| 경사↔수평 전환부의 **둥근 필렛** | 렌더·히트테스트·충돌면 좌표가 1e-9 이내로 일치해야 한다는 불변식(`spec4`)이 있다. 장식용 필렛은 "보이는 면"과 "부딪히는 면"을 어긋나게 하므로 넣지 않았다. 실제로 그 어긋남이 과거 결함 ②의 원인이었다. |
| **치수선**(양끝 화살표 + 점선 보조선) | 시뮬레이터에는 "표시할 치수"라는 개념이 없다. 사용자가 지정한 길이를 표기하는 기능을 추가한다면 그때 함께 넣는 편이 맞다. |
| **이전 위치 점선 윤곽** | 수능은 정지 그림이라 시간 경과를 점선으로 표현하지만, 시뮬은 실시간이라 필요 없다. `SN.ghostDash` 토큰은 남겨 두었다. |
| 물체 안 **기호 라벨(A, B)** | 시뮬에는 식별자가 없어 질량을 쓴다. 다만 글자가 상자를 넘치면 수능처럼 물체 **밖**으로 뺀다. |

## 4. 구현 방식 — SVG path + Canvas 렌더

`js/svg-shapes.js` 가 모든 요소 모양을 **SVG path `d` 문자열**로 만든다.

```
svgRect / svgCircle / svgPolyline / svgCoil
svgPulleyWheel / svgPulleyYoke / svgArrow / svgDimension / svgBand
```

- **화면**: `new Path2D(d)` 로 캔버스에 그린다 → 모양은 SVG 규격, 성능은 캔버스 그대로(60fps, 팬·줌 유지)
- **내보내기**: 같은 문자열을 `<path d="…">` 로 찍어낸다 (`js/capture.js` · `buildSceneSVG`)

즉 **보이는 그림과 내보낸 벡터가 같은 기하를 공유**한다. `촬영` 버튼은 이제 PNG가 아니라
흰 바탕·흑백 **SVG 파일**을 저장하므로 학습지·시험지에 그대로 붙일 수 있다.

### 용수철 코일을 지그재그와 구분하는 방법

축을 따라 진행하는 나선을 살짝 비스듬히 본 투영으로 그린다.

```
p(t) = 축·(c·t + a·sin t) + 수직·(b·cos t)
```

`a > c·π` 이면 한 바퀴 진행량보다 고리 반경이 커서 **고리가 서로 겹친다**.
그래서 사인파(지그재그)가 아니라 "감긴 코일"로 읽힌다. `svgCoil` 이 이 조건을
강제하고(`a = max(amp·0.5, c·π·1.25)`), `spec7 SN-COIL` 이 축 방향 **역행 구간 수**로
겹침을 검증한다 (사인파면 역행이 0회).

## 5. 검증 (`test/spec7.js`, 8항목)

| 항목 | 내용 |
|---|---|
| SN-BAND-LINE | 직선 바닥·천장·벽(양방향)·경사·마찰면에서 실체면 회색 띠가 충돌 법선 **반대쪽**을 덮는지 |
| SN-BAND-CURVE | ELBOW_H/V, ARC_UP/DOWN × 곡률 4종 전 구간 |
| SN-BAND-DIR | 띠가 향하는 화면 방향(바닥=아래, 천장=위, 벽=막히는 쪽) |
| SN-COIL | 코일 끝점 정확도, **고리 겹침**(축 역행 존재), 진폭 |
| SN-PULLEY | 동심원 반지름비 1 / 0.78 / 0.30 / 0.10, 요크 2줄·반폭·핀 위치 |
| SN-ARROW | 샤프트가 화살촉 앞에서 끝나는지, 삼각형 3점, 화살촉 반폭 |
| SN-SVG-EXPORT | 내보낸 SVG의 XML 유효성·viewBox·태그 균형, 흰 바탕, 마찰 라벨, **유채색 0개**, NaN 없음 |
| SN-SVG-GEOM | SVG 내보내기 좌표 = 화면 좌표 (물체 사각형 네 변 대조) |

전체 스위트 **156/156 통과**.
