# 물리 정합성 검증 보고서

역학 요소들의 가능한 조합을 대표 케이스로 뽑아, **실제 고전역학 공식과 수치 비교**하여
시뮬레이터의 동작을 검증하고 어긋난 부분을 수정한 기록.

- 검증 항목: **140개** (6개 스위트)
- 실행: `node test/run-all.js` (의존성 없음, Node 표준 라이브러리만 사용)
- 결과: 현재 코드 **140/140 통과**, 수정 전 원본 코드 **122/140 (18개 실패)**

---

## 1. 검증 방법

`test/harness.js`가 실제 `js/config.js` · `js/coords.js` · `js/elements.js` ·
`js/physics.js` · `js/hit-test.js` 를 **수정 없이 그대로** Node `vm` 컨텍스트에 올린다.
브라우저 DOM은 최소 스텁으로 대체하고, 물리 코드는 한 글자도 바꾸지 않는다.
따라서 검증 대상은 실제로 브라우저에서 도는 것과 **동일한 코드**다.

각 시나리오는

1. 씬을 구성하고 (`addRect` / `addFloor` / `addRope` / `addPulley` / `addSpring` / `addExtF` …)
2. `initPhysics()` 후 `simStep(FIXED_DT)`를 정해진 횟수만큼 돌리고
3. 결과 좌표·속도를 **해석적 공식값**과 비교한다.

`test/spec5.js`는 여기에 더해, 결과를 실제 렌더 변환
(`physToWorld` → `worldToScreen`, 앱 최초 로드 뷰포트 = 800×800 캔버스 · 400% 배율,
**1 m = 32 px**)에 통과시켜 **캔버스 픽셀 좌표**로 바꾼 뒤 공식 픽셀값과 비교한다.
대부분 항목이 **1 px 미만(3 cm 미만)** 오차로 일치한다.

> 크롬에서 직접 띄워 스크린샷을 찍는 방식은, 이 환경의 크롬이 로컬 서버
> (127.0.0.1 / localhost)에 `ERR_CONNECTION_REFUSED`로 접속하지 못해 사용할 수 없었다
> (외부 https는 정상, 서버는 `curl` 200 확인됨). 사용자 판단에 따라 Node에서
> 동일 코드를 구동해 좌표를 직접 읽는 방식으로 대체했고, 픽셀 검증은
> 렌더 변환을 그대로 적용해 유지했다.

---

## 2. 조합 매트릭스

요소 7종 + 중력 토글의 단독 동작과 상호 조합을 대표화했다.

| 축 | 다룬 변형 |
|---|---|
| 물체 | 사각형 / 원 (질량, 반발계수 e, 초기속도, 크기, 회전관성) |
| 바닥면 | LINE / ELBOW_H / ELBOW_V / ARC_UP / ARC_DOWN × 곡률 10종 × 마찰 on·off (μs, μk) |
| 실(Rope) | 물체↔물체, 물체↔고정점, 도르래 림, 도르래 center, 용수철 끝단, 직렬 2단, V자 2줄 |
| 도르래 | 고정 1개 / 고정 직렬 2개 / 움직도르래 / 한쪽만 연결 |
| 용수철 | 가로·세로, 양단 체결 / 한쪽 자유단 / 미체결(분리), 벽·천장·물체 대상, 자유 2체 |
| 외력 | 직접 부착 / 도르래 경유 / 0 N / 중력과 평형 |
| 힘 구간 | 단독 / 중첩 / 이탈 / 마찰과 경쟁 / 도르래 무시 |
| 중력 | ON / OFF |

### 스위트별 구성

| 스위트 | 항목 | 내용 |
|---|---|---|
| `spec.js` | 51 | 자유낙하·포물선·관성 / 반발계수 / 마찰 / 구름 / 물체충돌 / 힘구간 / 실·진자 / 도르래 / 용수철 / 외력 / 복합 |
| `spec2.js` | 38 | 미끄럼→구름 전이 / 적층 / e 조합 / 힘구간 조합 / 실 조합 / 외력 조합 / 용수철 조합 / 곡면 / 경계·안정성 |
| `spec3.js` | 24 | 경사면 반사각 / 벽 마찰 / 비스듬한 충돌 / 스핀 / 2체 용수철 / 실+충돌 / 극단 질량비 / 사거리·에너지 |
| `spec4.js` | 6 | 렌더 ↔ 히트테스트 ↔ 물리, 세 구현의 곡면 기하 일치 (곡률 10종 × 2종 × 기울어진 현) |
| `spec5.js` | 8 | 화면 픽셀 좌표 검증 |
| `spec6.js` | 13 | 단면 바닥 규약, 정지마찰 고정, 직렬 도르래, 남은 연결 조합 |

### 검증에 사용한 공식 (일부)

| 항목 | 공식 |
|---|---|
| 자유낙하·포물선 | `y = y₀ + v₀t − ½gt²`, `R = v₀²sin2θ/g`, `H = (v₀sinθ)²/2g` |
| 반발 | `v' = e·v`, 반등높이 `= e²h`, 물체쌍 `e = √(e₁e₂)` |
| 마찰 | `a = −μk·g`, 경사면 `a = g(sinθ − μk cosθ)`, 정지조건 `μs > tanθ` |
| 구름 | 경사면 `a = ⅔g sinθ`, 미끄럼→구름 `v_f = ⅔v₀`, 접촉점 각운동량 보존 |
| 충돌 | 운동량·에너지 보존, `v₁' = ((m₁−em₂)v₁ + (1+e)m₂v₂)/(m₁+m₂)`, 동질량 탄성 = 속도 직교 |
| 진자 | `T = 2π√(L/g)` |
| 용수철 | `T = 2π√(m/k)`, 2체 `T = 2π√(μ/k)`, `x_eq = mg/k`, `v_max = A√(k/m)` |
| 도르래 | Atwood `a = (m₁−m₂)g/(m₁+m₂)`, 도르래+평면 `a = m₂g/(m₁+m₂)`, 움직도르래 `a = g(W−2M)/(W+4M)` + 2:1 속도비 |

---

## 3. 발견하여 수정한 결함 5건

### ① 반발계수가 √e 로 적용됨 — 반등 높이가 공식과 다름
`js/physics.js` · `_resolveCircleFloor`, `_resolveRectFloor`

바닥 충돌에서 `e_c = Math.sqrt(el.e)` (사각형은 `Math.sqrt(el.e * 1.0)`)를 썼다.
패널의 `반발계수 e`는 물체↔바닥 **쌍의 반발계수**이므로 그대로 써야 한다.

| e | 반등 높이 (수정 전) | 반등 높이 (공식·수정 후) |
|---|---|---|
| 0.5 | 0.50 h | **0.25 h** |
| 0.25 | 0.25 h | **0.0625 h** |
| 0.8 | 0.89 h | **0.64 h** |

낙차 20 m·e=0.5 기준 반발속도가 13.88 m/s → **9.90 m/s**(공식값)로 교정됐다.
물체↔물체 충돌의 `√(e₁e₂)`는 두 물체 재질의 조합이므로 그대로 유지했다.
검증: `spec B2·B3`, `spec2 N2·N3`, `spec3 U2`, `spec5 PX4`

### ② 곡률 1.0(반원) ARC_DOWN이 물리에서만 언덕으로 뒤집힘
`js/physics.js` · `_arcPhysPoints` / `js/hit-test.js` · `_arcSamplePoints`

호의 스윕 방향을 **끝점 각도차의 부호**로 정하고 있었다. 그런데 `h = R·cos(θ/2)`는
θ=π에서 `20 × 6.12e-17 ≈ 1.2e-15`이고, 이는 좌표값 50의 배정밀도 ULP(7.1e-15)에
흡수되어 **중심이 현의 중점과 정확히 일치**한다. 그러면 각도차가 정확히 ±π가 되어
부호가 임의로 결정되고, ARC_DOWN(골짜기)이 물리에서만 언덕으로 뒤집혔다.

결과: 화면에는 골짜기가 그려지는데 충돌면은 언덕 → 골짜기에 놓은 물체가 지형
내부로 판정돼 **그대로 무한 낙하**했다 (테스트에서 y = −1050까지 떨어짐).

스윕 방향을 각도차 대신 **호의 볼록한 쪽(apex)** 으로 판정하도록 바꿨다.
`apex = mid ± n·(R − h)` 는 h의 부호나 0 여부와 무관하게 항상 정의된다.

같은 결함이 `hit-test.js`에도 있어 **클릭 선택 판정**과 **마찰 해치 렌더**가
그려진 곡선과 반대쪽을 가리키고 있었다 — 함께 수정했다.

`spec4.js`가 렌더(`ctx.arc` 의미를 그대로 재현한 참조 구현) ↔ 히트테스트 ↔ 물리
**세 구현의 좌표를 곡률 10종 × 2종 × 기울어진 현에서 직접 비교**하여
1e-9 이내 일치를 잠근다. 수정 전에는 6개 중 4개가 실패했다.

### ③ 단면(single-sided) 바닥의 뒷면 오판정 — 아래에서 위로 순간이동
`js/physics.js` · `_backFaceSkip`, `_depenetrateInitial`

바닥면은 법선 쪽에서만 실체라고 코드에 명시돼 있으나(`단면 충돌`),
구현은 **뒷면에 있는 물체도 표면 반지름 안에 들어오면 침투로 오판**해
앞면으로 밀어냈다. 즉 바닥 아래를 지나가던 물체가 바닥 위로 솟구쳤다.

재현: 바닥 y=50, 공을 y=40에서 위로 13.86 m/s로 발사 (이론 최고점 49.80, 바닥에
닿지 못함) → 수정 전에는 **y=50.5로 순간이동해 바닥 위에 안착**했다.

판정 기준을 **이번 서브스텝 시작 시점의 위치**로 바꿨다. `integrate()`가 방금
`v·dt`만큼 옮겼으므로 이전 위치는 `현재 − v·dt`로 정확히 복원된다.
- 이전에 앞면 → 처리 (접촉 침투는 물론, 한 스텝에 뚫고 지나간 고속 관통도 배출)
- 이전에 뒷면 → 무시 (뒤에서 온 물체는 통과)

> 속도 부호로 판정하면 **최고점(v≈0)에서 부호가 무너져** 그대로 솟구친다.
> 실제로 그 방식을 먼저 시도했다가 실패해서 위치 기준으로 바꿨다.

원은 중심, 사각형은 네 모서리 기준으로 앞/뒤를 판정한다 (경사면에 얹힌 사각형은
중심이 면 아래라도 위쪽 모서리가 앞면이기 때문).

추가로 `_depenetrateInitial()`을 `initPhysics()`에 넣어, 격자 스냅 때문에 경사·곡면에
조금 파묻힌 채 배치된 물체를 **시작 시 1회** 표면 밖으로 정렬한다. 속도를 0으로 둔 채
위치만 보정하므로 반발·마찰은 발생하지 않는다. 이 정렬이 없으면 위 판정이
"뒷면에서 시작"으로 오인해 물체가 지형을 통과한다.

검증: `spec6 AA1~AA4`, `spec2 T6`, `spec6 AC6`

### ④ 정지마찰 크리프 — 멈춰 있어야 할 물체가 계속 기어감
`js/physics.js` · `_resolveRectFloor`

정지마찰이 충분해 접선속도를 완전히 없애는 경우에도, `integrate()`가 **이미**
`v_t·dt`만큼 물체를 옮겨 놓은 뒤였다. 속도는 0으로 보이는데 위치만 매 서브스텝
흘러가는 형태다.

| 상황 | 크리프 속도 (수정 전) | 60초 이동 |
|---|---|---|
| 26.57° 경사면 (μs=0.9) | g·sinθ·dt ≈ 1.8 cm/s | ≈ 1.1 m |
| 수직 벽 (μs=0.9) | g·dt ≈ 4.1 cm/s | ≈ 2.4 m |

정지마찰 분기에서 그 서브스텝의 접선 변위를 되돌리도록 했다. 수정 후 60초간
이동량 **정확히 0**. 미끄러지는 중(운동마찰 분기)에는 적용하지 않으므로
`a = g(sinθ − μk cosθ)` 등 기존 마찰 동역학은 그대로다.

검증: `spec3 U4`, `spec6 AB1~AB3`

### ⑤ 직렬 도르래 2개에서 계 전체가 고착 (가속도 0)
`js/physics.js` · `_buildDynamicComponent`

실이 도르래 2개를 거치는 배치에서, 도르래마다 쌍 Atwood 제약
`d_i + d_j = 일정`을 적용하고 있었다. 이는 도르래가 **하나일 때만** 옳다.
도르래가 둘이면 가운데 구간이 양쪽에서 이중 계상되고, 그 구간은 고정 도르래
사이라 길이가 상수이므로 결국 양쪽 물체가 각각 "거리 일정"으로 묶여
**계 전체가 얼어붙었다**(a = 0).

올바른 모델은 런(run) 전체에 대한 총길이 제약 `Σd = L` 하나뿐이며,
코드에는 이미 그 KKT 선형해(`_solveDynamicComponent`)가 있었지만
**움직도르래가 있는 런에만** 쓰이고 있었다. 조건을
`움직도르래 포함 || 도르래 2개 이상`으로 넓혔다.

4 kg : 1 kg 직렬 2도르래에서 가속도 0 → **5.88 m/s²**(= (m₁−m₂)g/(m₁+m₂), 공식값)로 교정.
도르래 1개인 런은 검증된 기존 경로를 그대로 타므로 Atwood·도르래+평면 등은 영향 없다.
검증: `spec6 AC3`

---

## 4. 검증했으나 정상으로 확인된 항목 (일부)

- 갈릴레이 낙하(질량 무관), 뉴턴 1법칙, 포물선 사거리·최고점
- 물체 충돌: 동질량 속도교환, 질량비 1:3·1:2·1000:1, 완전비탄성 공통속도,
  운동량·운동에너지 보존, **동질량 탄성 비스듬 충돌 후 속도 직교**
- 구름: 경사면 `⅔g sinθ`, 미끄럼→구름 전이 `v_f = ⅔v₀`, 백스핀 역전 시
  **접촉점 각운동량 보존**, 마찰 없으면 ω=0
- 진자: 주기 `2π√(L/g)`, 실 길이 제약(오차 1.7e-7 m), 에너지 보존, 무중력 원운동
- 도르래: Atwood 3:1·5:2·1000:1, 도르래+평면, 움직도르래 정적평형(M=W/2)과
  2:1 속도비, 한쪽만 연결
- 용수철: SHM 주기, 2체 `μ` 주기, 정적 평형 `mg/k`, `v_max = A√(k/m)`,
  질량중심 보존, 자동 체결, 압축 반발, 미체결 인장 시 분리, k=0·k=500 경계
- 외력: `a = F/m`, 중력 평형, 방향 동결, 도르래 경유
- 안정성: 60초 정지 유지, 고속(80 m/s) 관통 없음, 질량 0·음수 가드, 3단 적층

---

## 5. 수정하지 않고 문서화한 사항 2건

### (가) 실 제약의 1차 에너지 소산 — 적분 정밀도 문제

진자를 30초 돌리면 역학적 에너지가 조금 줄어든다. 이는 "위치 투영 제약 + 1차
반음적 오일러"의 이산화 소산이며, **dt에 정확히 비례해 0으로 수렴**한다.

| SUBSTEPS | dt | 30초 에너지 손실 |
|---|---|---|
| 1 | 1.67e-2 | 5.77 % |
| 2 | 8.33e-3 | 3.37 % |
| 4 (현재) | 4.17e-3 | **1.85 %** |
| 8 | 2.08e-3 | 0.97 % |
| 16 | 1.04e-3 | 0.50 % |
| 32 | 5.21e-4 | 0.25 % |

절반씩 정확히 줄어드는 1차 수렴이므로 **물리 모델의 오류가 아니라 적분 정밀도**다.
`spec2 T5`가 30초 드리프트 상한(2.5%)과 "에너지가 늘지 않을 것"을,
`spec2 T7`이 1차 수렴 자체를 검증한다.
더 줄이고 싶으면 `CONFIG.SUBSTEPS`를 8로 올리면 손실이 절반이 된다 (물리 비용 2배).
현재 씬 규모에서는 부담이 크지 않지만, 성능 정책에 해당하므로 바꾸지 않았다.

### (나) 바닥면은 단면(one-way) — 그리는 방향이 법선을 정한다

`getPhysicsSegments`의 법선은 A→B의 **반시계 90°** 이고, 물체는 법선 쪽에서만
바닥과 충돌한다. 이는 코드에 명시된 설계 결정이다(`강제 뒤집기 없음 - 단면 충돌`).
결함 ③ 수정으로 구현이 이 설계와 **일치**하게 됐지만, 설계 자체의 결과로:

- 바닥을 왼→오로 그리면 법선이 위 → 위에서만 막힌다. 오→왼으로 그리면 아래.
- 천장을 만들려면 **오른쪽에서 왼쪽으로** 그려야 한다.
- 뒷면에서 표면을 완전히 통과한 물체는 앞면으로 나와 그 위에 얹힌다
  (게임의 one-way 플랫폼과 같은 거동).
- UI에 법선 방향 표시가 없어서, 사용자가 의도와 반대로 그리면 물체가 통과한다.

양면(solid) 충돌로 바꾸는 것은 기존 씬의 동작을 바꾸는 **설계 변경**이라
임의로 손대지 않았다. 필요하시면 별도로 진행하겠습니다. 최소 개선안으로는
바닥면 선택 시 법선 방향 화살표를 표시하거나, 속성 패널에 "면 뒤집기" 버튼을
추가하는 방법이 있습니다.

---

## 6. 파일

```
test/
├── harness.js     실제 js/*.js 를 그대로 로드하는 검증 하네스 + 씬 빌더 + 러너
├── spec.js        기본 역학 51
├── spec2.js       조합 2차 38
├── spec3.js       조합 3차 24
├── spec4.js       기하 일치 6
├── spec5.js       픽셀 검증 8
├── spec6.js       단면 규약·정지마찰·직렬 도르래 13
└── run-all.js     전체 실행 (node test/run-all.js [--verbose])
```

수정된 소스: `js/physics.js`, `js/hit-test.js`
