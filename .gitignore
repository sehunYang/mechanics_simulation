# OMC (oh-my-claudecode) 로컬 상태 — 저장소에 포함하지 않음
.omc/

# OS / 에디터
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# 로그·임시
*.log
node_modules/

# 로고 원본 (2380px, ~900KB) — 아이콘은 assets/logo.png(512px) 에서
# 재생성 가능하므로 저장소·배포에는 넣지 않는다.
양세훈 로고.png
